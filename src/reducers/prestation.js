import {} from '../actions';

const initialState = {};

const prestation = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default prestation;
