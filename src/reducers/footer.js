import {} from '../actions';

const initialState = {};

const footer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default footer;
