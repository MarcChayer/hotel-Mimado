import {} from '../actions';

const initialState = {};

const restauration = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default restauration;
