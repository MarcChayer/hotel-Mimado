import {} from '../actions';

const initialState = {};

const mentionslegales = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default mentionslegales;
