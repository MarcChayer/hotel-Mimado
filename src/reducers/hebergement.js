import {} from '../actions';

const initialState = {};

const hebergement = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default hebergement;
