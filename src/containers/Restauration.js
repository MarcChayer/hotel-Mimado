import { connect } from 'react-redux';
import Restauration from 'src/components/Restauration';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(Restauration);
