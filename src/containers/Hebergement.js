import { connect } from 'react-redux';
import Hebergement from 'src/components/Hebergement';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(Hebergement);
